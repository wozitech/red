global.console = {
  error: jest.fn(),
  warn: jest.fn(),
  log: jest.fn()
};

describe('Robots Set', () => {
  beforeAll (() => {});
  beforeEach (() => {});

  it('should initialise with no robots', () => {
  });

  it('should have one robot', () => {
  });

  it('should have two robots', () => {
  });

  it('should have ten robots', () => {
  });
});

describe('Robot Scent', () => {
  beforeAll (() => {});
  beforeEach (() => {});  
  
  it('should find no scent', () => {
  });

  it('should find scent', () => {
  });
});